import { SetComponentDirective } from './set-component.directive';

describe('SetComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new SetComponentDirective();
    expect(directive).toBeTruthy();
  });
});
