import { CheckRolesDirective } from './check-roles.directive';

describe('CheckRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckRolesDirective();
    expect(directive).toBeTruthy();
  });
});
